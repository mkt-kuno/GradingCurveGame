{
  "name": "grading-game",
  "version": "3.5.1",
  "scripts": {
    "build": "bun build src/game.ts --outfile dist/game.js --target browser && bun run scripts/build.ts",
    "serve": "bun run server.ts",
    "dev": "bun run build && bun run serve"
  }
}
