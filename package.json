{
  "name": "grading-game",
  "version": "2.0.0",
  "scripts": {
    "build": "bun build src/game.ts --outfile dist/game.js --target browser",
    "serve": "bun run server.ts",
    "dev": "bun run build && bun run serve"
  }
}
